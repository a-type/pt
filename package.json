{
	"name": "@pt/root",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "turbo run dev",
		"build": "turbo run build",
		"test": "turbo run test",
		"prisma": "pnpm --filter @pt/prisma run prisma",
		"generate": "pnpm --filter @pt/verdant run generate",
		"preflight": "pnpm --filter @pt/verdant run preflight"
	},
	"devDependencies": {
		"turbo": "^1.6.1"
	}
}
